# Node
node_modules/

# Prompt Registry (dado local, não versionar)
generator/prompt_registry/prompt_registry.jsonl

# Suítes geradas (são o produto final, cada usuário gera as suas)
generated-suite/
generated-suite-*/

# Ambiente
.env
